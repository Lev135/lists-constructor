\documentclass[<!= f.optionsToString(f.documentClassOptions(pars)) !>]{article}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage[<!= f.optionsToString(f.geometryOptions(pars)) !>]{geometry}
<!~ obj.packages : package !>
\usepackage{<!= package !>}
<!~!>

\newcommand{\tmp}[1]{\noindent\par\textit{#1: }\nopagebreak\par}

\begin{document}
    \section{Задача \textnumero <!= obj.id !>}
    \tmp{Условие}
        <!= obj.statement.body !>
    \tmp{Ответ}
        <!= obj.answer !>

    <!~ obj.solutions :s !>
    \tmp{Решение}
        <!= s.body !>
    \tmp{Класс}
        <!= s.grade !>
    <!~!>

    \noindent\rule{\textwidth}{0.2pt}
    Created by <!= obj.author.name !>

\end{document}
