// Редактирование задачи task/edit:

// При GET запросе сервер генерирует страницу редактирования задачи,
// передавая ей json с полной информацией о задаче (см. task/view)

// При POST запросе клиент возвращает информацию о результате изменений,
// в виде новой полной информации о задаче
// (редактирование комментариев не предполагается,
// но возможно, можно будет добавлять (для того, чтобы ответить
// человеку, предложившему изменения, например))
